# Phase 1 Spike Notes — Frozen Baseline Snapshot

Generated by PLAN-1.1 execution. Later phases diff against this document.

---

## R1 — NuGet 0.9.18 Availability

**Verdict: GREEN**

All 8 DNWQ packages (main + 7 transports) confirmed published at version 0.9.18 on NuGet.org.

| Package | 0.9.18 | Last 3 published |
|---|---|---|
| `DotNetWorkQueue` | YES | 0.9.18, 0.9.30, 0.9.31 |
| `DotNetWorkQueue.Transport.SqlServer` | YES | 0.9.18, 0.9.30, 0.9.31 |
| `DotNetWorkQueue.Transport.PostgreSQL` | YES | 0.9.18, 0.9.30, 0.9.31 |
| `DotNetWorkQueue.Transport.SQLite` | YES | 0.9.18, 0.9.30, 0.9.31 |
| `DotNetWorkQueue.Transport.LiteDb` | YES | 0.9.18, 0.9.30, 0.9.31 |
| `DotNetWorkQueue.Transport.Redis` | YES | 0.9.18, 0.9.30, 0.9.31 |
| `DotNetWorkQueue.Transport.Shared` | YES | 0.9.18, 0.9.30, 0.9.31 |
| `DotNetWorkQueue.Transport.RelationalDatabase` | YES | 0.9.18, 0.9.30, 0.9.31 |

**Note:** Every package skips 0.9.19–0.9.29 on NuGet. Those versions were tagged in git but never published. 0.9.18 is simultaneously the last .NET Framework 4.8-compatible release and the highest published non-net10-only version. No competing "newer 4.8" release exists.

---

## R2 — AppMetrics Touch-Point Inventory

Grep pattern: `App\.Metrics|IMetricsRoot|IMetrics\b|DotNetWorkQueue\.AppMetrics` across `Source/Examples/`. 88 total hits. Editable-file hits:

### .cs source files (8 files)

Shared (in `Source/Examples/Shared/ConsoleSharedCommands/Commands/`):
- `SharedCommands.cs`
- `ConsumeMessageAsync.cs`
- `ConsumeMessage.cs`

Per-transport `SendMessage.cs` in each Producer project:
- `Source/Examples/LiteDb/Producer/LiteDbProducer/Commands/SendMessage.cs`
- `Source/Examples/PostGresSQL/Producer/PostGresSQLProducer/Commands/SendMessage.cs`
- `Source/Examples/Redis/Producer/RedisProducer/Commands/SendMessage.cs`
- `Source/Examples/SQLite/Producer/SQLiteProducer/Commands/SendMessage.cs`
- `Source/Examples/SQLServer/Producer/SqlServerProducer/Commands/SendMessage.cs`

AppMetrics wiring is confined to the Producer SendMessage path and the shared ConsumeMessage bases. No Consumer, ConsumerAsync, or QueueCreation files appear. Phase 3 code-strip scope is bounded.

### packages.config files referencing App.Metrics (17 files)

All 15 runnable project `packages.config` files plus `Shared/ConsoleSharedCommands/packages.config` and `Shared/ConsoleView/packages.config`. Every file carries 6 `<package id="App.Metrics..." version="4.3.0" targetFramework="net472" />` entries covering: `App.Metrics`, `App.Metrics.Abstractions`, `App.Metrics.Concurrency`, `App.Metrics.Core`, `App.Metrics.Formatters.Ascii`, `App.Metrics.Formatters.Json`.

### .csproj files referencing App.Metrics (17 files — same projects)

Each csproj holds corresponding `<Reference Include="App.Metrics..." ...>` entries with `<HintPath>..\..\..\packages\App.Metrics.4.3.0\lib\net461\App.Metrics.dll</HintPath>` style paths. Phase 3 must remove both the package references and the csproj `<Reference>` items.

### App.config files referencing App.Metrics (15 files)

Every runnable project's `App.config` has `<assemblyBinding>` → `<dependentAssembly>` → `<assemblyIdentity name="App.Metrics..."` binding-redirect entries. Phase 3 must strip these.

### Source/Examples/packages/ cache (not tracked)

These files live in the NuGet package cache and will vanish when Phase 3 runs `nuget restore` against a package list without AppMetrics. No edit action required.

---

## Baseline Warnings

**Warning count: 0**

MSBuild command used for measurement:

```
MSBUILD='/mnt/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe'
SLN='F:\git\dotnetworkqueue.examples\Source\Examples\Examples.sln'
"$MSBUILD" "$SLN" /t:Restore /p:Configuration=Release /v:minimal
"$MSBUILD" "$SLN" /t:Build   /p:Configuration=Release /v:normal
```

MSBuild version: 18.4.0+6e61e96ac for .NET Framework

Build summary from `/v:normal` run:
- `0 Warning(s)`
- `0 Error(s)`

**Unique warning list:** (none — solution builds with zero warnings)

**Note on path translation:** The plan specified a WSL path (`/mnt/f/git/.../Examples.sln`) but MSBuild.exe is a Windows binary that rejects WSL paths as unknown switches. The correct invocation uses `wslpath -w` to obtain the Windows-native path (`F:\git\...\Examples.sln`). This deviation is documented here for all future phases that invoke MSBuild from WSL.

---

## .gitattributes Outcome

**Task 1 result:** `.gitattributes` committed in commit `700a7c3` with subject `shipyard phase 1: add .gitattributes for line-ending discipline`.

- `git add --renormalize .` (first pass, after staging `.gitattributes`): **no additional files staged**. Only `.gitattributes` itself was in the commit. The existing index already stored CRLF line endings (Windows-authored repo), so the `* text=auto eol=crlf` rule produced zero normalization deltas against the current index. Files staged: **1** (`.gitattributes` only).
- `git add --renormalize .` (second pass, idempotency check): **clean** — zero staged changes. The rule is fully applied and stable.

The WSL git warning `LF will be replaced by CRLF` appeared when `.gitattributes` itself was staged, confirming the rule is active and `.gitattributes` will be stored with CRLF in the working tree on next checkout — correct behavior for a Windows-authored repo.

---

## Phase 1 Verdict

GO — Phase 2 may begin
